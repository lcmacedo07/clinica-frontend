// import { url } from "@/global";

// export default defineEventHandler(async event => {
//     const { data: api } = await useFetch(`${this.$config.public.baseURL}/users`);
//     return {
//         api: api,
//     }

// })